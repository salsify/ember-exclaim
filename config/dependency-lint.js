/* eslint-env node */
'use strict';

module.exports = {
  allowedVersions: {
    // these versions are all compatable
    'ember-cli-clipboard': '0.8.1 || 0.11.1',

    // 2.1.0 introduced the `is-empty` helper
    'ember-truth-helpers': '2.0.0 || 2.1.0',

    // these versions are all compatable
    'ember-assign-polyfill': '2.4.0 || 2.6.0',
  },
};
