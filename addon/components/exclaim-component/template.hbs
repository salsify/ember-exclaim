{{#component wrapper
  spec=unwrappedSpec
  env=effectiveEnv
  config=resolvedConfig
}}
  {{#component spec.path
    config=resolvedConfig
    env=effectiveEnv
    as |spec overrideEnv|
  }}
    {{~exclaim-component spec=spec env=effectiveEnv overrideEnv=overrideEnv wrapper=wrapper~}}
  {{/component}}
{{/component}}
