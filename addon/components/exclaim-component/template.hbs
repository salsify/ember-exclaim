{{#component
  @wrapper
  componentSpec=this.unwrappedSpec
  env=this.effectiveEnv
  config=this.resolvedConfig
}}
  {{#component
    @componentSpec.path
    config=this.resolvedConfig
    env=this.effectiveEnv
    as |componentSpec overrideEnv|
  }}
    {{~exclaim-component
      componentSpec=componentSpec
      env=this.effectiveEnv
      overrideEnv=overrideEnv
      wrapper=@wrapper
    ~}}
  {{/component}}
{{/component}}
